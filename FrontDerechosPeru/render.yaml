services:
  - type:web
    name: constitucionPeru
    env: node
    plan: free
    buildCommand: "npm ci && npm run build"
    startCommand: "node dist/FrontDerechosPeru/server/server.mjs"
    region:lima
    autoDeploy:true