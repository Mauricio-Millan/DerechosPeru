fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pgvector>=0.3
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
# Verificación de JWT de Supabase (Sprint 2 — auth)
python-jose[cryptography]>=3.3
# Embeddings locales y gratuitos (Etapa 1)
sentence-transformers>=3.0
# Ingesta de constituciones por PDF (Sprint 6 — M8)
pypdf>=5.0
httpx>=0.27
# multipart: subida del PDF (UploadFile / Form)
python-multipart>=0.0.9
# Opcional: solo si EMBEDDING_PROVIDER=openai
# openai>=1.40
